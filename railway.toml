[build]
builder = "nixpacks"
nixpkgsVersion = "nixpkgs-unstable"

[build.nixpacks]
python = "3.11"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"

[[crons]]
name = "daily-scraper"
command = "python3 scraper/daily_scraper.py"
schedule = "0 2 * * *"